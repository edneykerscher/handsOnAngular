import { DataCustomizadaPipe } from './data-customizada.pipe';

describe('DataCustomizadaPipe', () => {
  it('create an instance', () => {
    const pipe = new DataCustomizadaPipe();
    expect(pipe).toBeTruthy();
  });
});
