import { PercentualPipe } from './percentual.pipe';

describe('PercentualPipe', () => {
  it('create an instance', () => {
    const pipe = new PercentualPipe();
    expect(pipe).toBeTruthy();
  });
});
